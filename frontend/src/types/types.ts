export type BlockCodeType = {
  id: string;
  title: string;
  code: string;
  href: string;
};

export type HeaderType = {
  tabName: string;
  title: string;
  subTitle?: string;
};

export type BlockCodeCardType = {
  title: string;
  href: string;
};
